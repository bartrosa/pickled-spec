# Rule sets

Example rule sets are illustrative. Bring your own rule sets in YAML following
the schema in [`packages/pickled-rules/README.md`](../README.md).

## Layout

```
rulesets/
└── examples/
    ├── team-api-conventions.yaml
    └── code-review-checklist.yaml
```
