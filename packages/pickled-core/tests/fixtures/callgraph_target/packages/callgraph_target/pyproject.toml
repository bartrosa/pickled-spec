[project]
name = "callgraph-target"
version = "0.0.1"
requires-python = ">=3.11"

[project.scripts]
callgraph = "callgraph_target.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/callgraph_target"]
